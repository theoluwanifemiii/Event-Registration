import { registerRootComponent } from 'expo';

import App from './App';

// registerRootComponent is the entry point to start the app
// If you are not using Expo Go, you should import the entry point file
// in the root directory and call registerRootComponent(App) there.
registerRootComponent(App);